 apiVersion: networking.k8s.io/v1
 kind: Ingress
 metadata:
   name: isvaconfig
   annotations:
     nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
 spec:
  ingressClassName: nginx-apps
  tls:
  - hosts:
    - isvaconfig-aac.apps.svam.nonprod.pvcombank.io
  rules:
  - host: isvaconfig-aac.apps.svam.nonprod.pvcombank.io
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: isvaconfig-aac
            port:
              number: 9443
